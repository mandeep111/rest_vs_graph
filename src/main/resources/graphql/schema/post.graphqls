type Post {
    id: ID!
    title: String!
    content: String
    url: String
    imageUrl: String
    platform: String
    source: String
    sentimentScore: Float
    isActive: Boolean!
    publishedAt: String
    category: Category
    author: Users
}

type Query {
    post(id: ID!): Post
}
